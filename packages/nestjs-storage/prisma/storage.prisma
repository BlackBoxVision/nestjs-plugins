model StoredFile {
  id           String   @id @default(cuid())
  key          String   @unique
  bucket       String
  provider     String
  mimeType     String
  size         Int
  originalName String
  uploadedBy   String?
  metadata     Json?
  createdAt    DateTime @default(now())
}
