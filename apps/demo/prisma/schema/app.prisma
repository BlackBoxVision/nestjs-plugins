model Item {
  id          String   @id @default(cuid())
  name        String
  description String?
  imageKey    String?
  createdBy   String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}
